PORT=3000
MCP_AUTH_TOKEN=replace-with-a-long-random-token
# 百炼 streamableHttp 使用标准流式响应；仅当客户端明确要求 JSON 时改为 true
MCP_JSON_RESPONSE=false
# 浏览器客户端需要跨域时填写，多个地址用英文逗号分隔
CORS_ALLOWED_ORIGINS=https://your-domain.example
